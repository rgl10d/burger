<h1>Eat-Da-Burger</h1>

<img src="https://www.pinclipart.com/picdir/big/492-4929476_burger-vector-with-transparent-background-cheeseburger-clipart.png" alt="burger">

<div class="list-container">
<h2 class="list-titles">Burgers to be devoured:</h2>
<ul class="lists" id="uneaten-list">
  {{#each burgers}}
  {{#unless devoured}}
  {{> burgers/burger-block devoured=false}}
  {{/unless}}
  {{/each}}
</ul>
</div>

<div class="list-container">
  <h2 class="list-titles">Devoured Burgers:</h2>
  <ul class="lists" id="devoured-list">
    {{#each burgers}}
    {{#if devoured}}
    {{> burgers/burger-block devoured=true}}
    {{/if}}
    {{/each}}
  </ul>
</div>

<div id="new-burger-container">
  <h2 id="new-burger-title">Add a new Burger</h2>
  <form class="create-form">
    <div class="form-group">
      <label for="ca">Burger Name:</label>
      <input type="text" id="ca" name="burger_name">
      <button type="submit">Add Burger</button>
    </div>
</div>
</form>